name: Check
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install deps
        run: |
          if [ -e yarn.lock ]; then
          npm install --legacy-peer-deps
          fi
          npm ci --legacy-peer-deps
      - name: Lint
        run: nmp run lint
  
  
